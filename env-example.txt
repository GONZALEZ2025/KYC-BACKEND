# Server
PORT=8080
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com,https://your-wordpress-site.com
JWT_SECRET=replace_with_long_random_string

# Storage driver: local | gdrive
STORAGE_DRIVER=gdrive
LOCAL_STORAGE_DIR=./data

# Google Drive (service account)
GDRIVE_SERVICE_ACCOUNT_EMAIL=
GDRIVE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GDRIVE_FOLDER_ID=

# Database (SQLite file by default)
SQLITE_PATH=./data/app.sqlite

# Crypto at rest
AES_KEY_HEX= # 64 hex chars (256-bit key). Generate with: openssl rand -hex 32

# Email (SendGrid)
SENDGRID_API_KEY=
EMAIL_FROM=no-reply@yourdomain.com

# SMS/WhatsApp (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_SMS_FROM=+1xxxxxxxxxx
TWILIO_WA_FROM=whatsapp:+1xxxxxxxxxx

# Sanctions/PEP (optional)
SCREENING_PROVIDER=none
SCREENING_API_KEY=
